type Query {
  hello: String!
  helloWithName(name: String): String!
  peopleNumber: Int!
  # Lista de los elementos
  booksList: Result!
  peoplesList: Result!
  # Elementos seleccionados
  book(id: ID!): Result!
  people(id: ID!): Result!
}
